
* 良いコード＝読みやすいコード、は正しい
* ただし抽象的すぎて実装中の判断軸にならない
* 書いている最中に「読みやすいか」は自分では判断できない
* 数日後に見返して分かる、では実務では遅い
* 実装中に立ち戻れる基準が必要
* コードを読む時間の大半は値を考えている時間
* 今この変数に何が入っているかを考えている
* if や for 自体が難しいわけではない
* 値が分からないと難しく感じる
* 理解とは値の追跡作業
* 最短時間で理解できるとは何か
* 最短時間で値を正しく予測できること
* 両者はほぼ同義
* 変数の値を予測しやすくするという軸
* 実装中に使える判断基準
* 読みやすさの下位概念としてちょうどいい
* 抽象と具体の距離が近い
* ミュータブルかイミュータブルかの判断
* この軸で考えると腑に落ちる
* 好みや流派の話ではなくなる
* まず減らすべきは予測対象
* 予測すべき変数の数
* 変数が多いほど覚えることが増える
* 覚えることが増えるほど予測は外れる
* 予測が外れると不安になる
* 不要な変数は消す
* 他から簡単に計算できる値は保持しない
* 減らすこと自体が目的ではない
* 1行に詰め込みすぎると式が読めなくなる
* スコープは思考の視野
* 視野が狭いほど正確に考えられる
* 狭いスコープの変数は多くてもいい
* 広いスコープの変数は少なくしたい
* 関数に切り出す
* 変数の影響範囲を隔離できる
* 読む側は他の場所を無視できる
* 関数の外の変数を考えなくていい
* 20行程度なら全体を把握できる
* 関数の役割が明示されていると助かる
* 引数が何を受け取るか
* 返り値が何を返すか
* 型がそれを補強する
* コメントが意図を固定する
* 関数の中身を読まずに使える
* 関数内部の変数を追わなくていい
* 追跡対象が減る
* 引数以外の状態に依存する関数
* 同じ入力で結果が変わる
* 結果の理由を探しに行く必要が出る
* DB やグローバル変数を見に行くことになる
* 思考が分断される
* ステートレスにできないか考える
* 状態を持つ場所を限定できないか考える
* 変数名は値の説明
* 型は値の制約
* 型があると間違った予測でエラーになる
* 予測ミスに気づける
* 曖昧な名前の変数
* 使うたびに再解釈が必要
* 認知コストが積み上がる
* 値が変わらない変数は安心できる
* 定義箇所だけ見ればいい
* エディタで一瞬で飛べる
* それ以降は疑わなくていい
* 値を変えたい場合
* 新しい変数を作る
* 古い変数は捨てる
* 多くの変数は使い捨てで問題ない
* 初学者の頃の感覚
* 変更できないのは不便に見える
* python のタプルへの違和感
* 慣れると見え方が変わる
* 勝手に変わらないこと自体が情報
* それが予測可能性になる
* 値が頻繁に変わるコード
* 直感的に不安になる
* 大抵その感覚は正しい
* 特に避けたいケース
* 引数を書き換える関数
* 内部で状態が変わるメソッド
* 変更箇所が分からない
* 状態変更が必要な場合
* 変更箇所を限定する
* 変更専用の操作にする
* 名前で何が変わるか示す
* コメントで補足する
* ORM の更新操作
* 値を変える責務が明示されている
* 予測可能性の設計
* React の単方向データフロー
* 状態変更箇所を減らす発想
* ステートレスコンポーネント
* 値の流れが追いやすい
* Vue は油断すると追いづらくなる
* DDD のエンティティ
* DDD の値オブジェクト
* 値オブジェクトはイミュータブル
* 偶然ではない
* テクニック暗記は目的ではない
* 判断基準を持つことが目的
* 変数の値を予測しやすいか
* 常に自問する
* ノートブックの捨てコード
* 行数が増えると読めなくなる
* アプリやシステムでは致命的
* 分散環境では状態共有が難しい
* 予測可能性はさらに重要
* 読みやすさは感覚論に見える
* 実際はかなり機械的
* 値が見えるか
* 変化が追えるか
