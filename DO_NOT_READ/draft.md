* コードは何度も読まれる
* 実務経験上、この方向性は正しい
* 4〜5年でようやく感覚が掴めた
* それ以前は重要でない点に注力していた
* 他人（未来の自分）が理解しやすくすべき
* 一般論：良いコード＝他人が読みやすい
* 理解しやすさは時間を置かないと分からない
* コードを書く・読む経験を通じて最重要だと感じた
* より具体的な原則を探し続け、辿り着いた原則が「変数の値を推測しやすくする」
* これを軸に考えると他の方法論の優先度もよく整理できる
* 本ブログではこれについて説明する
* 変数名で値を説明するとわかりやすい
* ただし常に詳細名が最適ではない
* mapの中みたいな狭いスコープでは、変数名は短い方が読みやすい場合がある
* 長すぎる名前は処理理解を妨げる
* 判断基準は「値を推測しやすいか」
* スコープが狭ければ名前は多少雑でもよい
* 文脈で値が明らかなら短い方が良い
* まずは具体例 - 変数に適切な名前をつける - でイメージを掴んでもらう
* コード読解＝変数値の追跡
* 関数読解＝引数と返り値の把握
* 返り値理解には内部変数の追跡が必要
* 「変数の値が推測しやすい」＝「コードが読みやすい」
* よって推測しやすさが可読性になる
* 推測しやすくする基本戦略は2段階
* 「推測対象の変数を減らす」→ 「残った変数を分かりやすくする」
* 最優先は変数自体を減らす
* 変数がなければ推測不要
* 規模・用途を問わず有効
* 不要なコード・変数を削除
* 実装をシンプルに保つ
* 重複ロジックは共通化
* map等で中間変数を減らす
* 変数のスコープを狭める
* 全体数が減らなくても局所理解は楽になる
* 主手段は関数・メソッド化
* 関数外の変数を無視できる
* メソッドでも同様
* 広いスコープの変数は減らす
* 短い関数内の変数は多くても問題ない
* 引数・返り値をコメントや型で説明する
* 使い方が分かれば実装を読まなくてよい
* 専用記法（JSDoc等）を使う
* 自明なことはコメントしない
* コメントは少ない方が安全
* 実装とズレると致命的
* 名前から分からない前提だけ書く
* 出力を引数以外に依存させない
* 同じ引数→同じ返り値
* ステートレス関数
* 外部状態依存は推測コスト増
* 実行環境依存は特に追えない
* 命名と型定義で値を説明する
* 名前と型で値が即イメージできる
* 型定義はIDE支援を最大化
* ホバーで確認できる
* 補完で実装が速くなる
* anyは読みにくさの元
* 変数の値を変更しない
* 再代入・状態変更は推測を難しくする
* 不変なら定義を見るだけで済む
* イミュータブル構造を活用
* Pythonタプル、Rust設計は参考
* 全不変は現実的でない場合もある
* 値を変更するなら見える場所で
* 定義から離れた再代入は避ける
* 引数の書き換えは特にNG
* クラス状態変更は専用化
* 状態変更が分かる命名
* get系では状態を変えない
* 判断基準を持つこと
* 「値を推測しやすいか」で選択できる
* 新しい方法も自分で導ける
* 技術は目的理解があって初めて使える
* 本質は手法暗記ではない
